"""
Tests for pipeline.gate (Pass A — structural validator).

These tests pin down the six rules. If a rule needs to change, the test
must change too — that's the whole point of the gate. Don't lower a limit
or a minimum to make a card pass; update the card and the spec together.
"""
